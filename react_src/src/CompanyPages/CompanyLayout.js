import React, { Component } from 'react';
import styled from 'styled-components';

import { Layout, CompanyHeader } from '../components';

const CompanyLayout = () => <Layout header={CompanyHeader}/>;

export { CompanyLayout };